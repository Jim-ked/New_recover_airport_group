# Web/API runtime. Keep implementation on basic Flask APIs compatible with both the
# project's older Python deployment line and newer Linux/Kylin environments.
Flask>=2.3,<4

# The real solver is an explicit runtime layer because its wheel/SCIP packaging must be
# validated per target platform. Install requirements-solver.txt for Run execution.

# Programmatic offline PDF result reports. CSV export uses the Python standard library.
reportlab>=4,<5

# Cross-platform production WSGI host for the formal Windows/Linux startup entrypoint.
waitress>=3,<4
