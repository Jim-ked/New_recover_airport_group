# F3 Indicator 指标管理状态

## 范围

F3 完成独立的指标管理产品面，继续沿用 F0 共享 Shell / tokens / components，不把指标管理重新接回算法运行或结果分析。

## 默认指标集

- 系统初始化自动种子 `默认指标集 V1.1`。
- 首次进入 `/indicators` 时默认打开该指标集；不以空白树作为初始状态。
- 当前冻结基线为 3 个一级、14 个二级、58 个三级，共 75 个节点。
- 一级：机场保障节点能力、机场群保障能力、区域顽存能力。
- 区域顽存能力保留五维：任务出动与执行效率、资源保障效能、节点网络协同效果、防护能力、应急响应能力。
- 任务完成率、专业物资保障率、群内机场分布均衡度为核心保护三级指标。
- 默认指标集是系统稳定基线，本身只读。

## 用户自建指标集

- 默认指标集不是唯一指标集。
- 具备 `indicators.write` 权限的用户可以基于任一已发布指标集创建独立草稿。
- 新建指标集复制既有一级/二级结构、三级定义与核心保护属性；用户在草稿中维护自己的非核心三级指标。
- 用户可修改/启停/删除非核心三级指标，也可新增三级指标。
- 一级、二级节点不开放任意结构设计；核心三级指标不可修改/删除。
- 自建草稿发布后成为普通 `published` 指标集，`is_default=false`，不会替换系统默认 V1.1。
- 已发布自建指标集定义只读；继续修改时再基于其创建新的草稿。

## 专家评分与权重

- 专家数量不固定，管理员可新增、修改、删除专家；已有 submitted 评分的专家受引用保护。
- 仅三级指标参与 0–100 专家评分。
- 每个专家/指标集使用一张聚合评分表，支持 draft / submitted 与 revision 乐观并发。
- submitted 必须覆盖当前指标集中全部启用的三级指标。
- 权重规则冻结为：对每个三级指标取所有已提交专家的算术平均分，再在同一二级指标下对启用三级子项归一化，使同组权重和为 1；同组均值全部为 0 时等权。
- draft 评分不参与权重计算。
- 指标权重不作为 Run 的优化权重，也不让前端自行计算运行指标值。

## 页面与交互

- 一级导航真实入口 `/indicators`。
- 顶部：指标集选择、系统默认/草稿/已发布状态、新建指标集、发布草稿。
- 左侧：一级 -> 当前一级下的二级 -> 当前二级下的三级，避免一次铺开 58 个三级节点形成高密度图。
- 右侧统一工作区：节点详情 / 专家评分。
- 三级节点详情显示类型、单位、方向、启用状态、只读权重和核心保护信息。
- 自建草稿中才出现编辑/新增入口；默认和已发布集不伪装成可编辑。
- 专家评分按二级分组录入，但提交时仍是完整三级评分表；显示已填写 X/Y 进度。
- 专家管理使用独立小型弹层，不扩大为额外一级页面。

## 不做的内容

- 不提供“切换系统默认指标集”；系统默认 V1.1 始终保留。
- 不提供空白一级/二级结构设计器，避免把模块扩展成复杂本体编辑器。
- 不在指标管理显示 Run 指标值、任务执行结果或结果分析图表。
- 不把基础数据字段、算法变量、公式绑定塞入 IndicatorNode schema。

## 验证

- 默认 seed 有结构数量与关键名称冻结测试。
- 自建指标集发布后不替换系统默认，有后端回归测试。
- F3 前端契约覆盖：真实一级导航、默认+自建指标集、三级浏览/L3 编辑、专家评分、归一化权重、无 Run/Results API 越界。
- `indicators.css` 显式字号下限 10px。
- 真实浏览器最终视觉仍按统一计划在 Flask + 本地资源环境做人工验收。
